# Rails
--ignore-dir=coverage
--ignore-dir=vendor
--ignore-dir=public

# Ruby
--type-add=css=scss
--type-add=ruby=.haml,.rselm,.feature,.ru,.lock
--type-set=coffeescript=.coffee
--type-set=coffee=.coffee

# Don't search generated files
--ignore-file=ext:log
--ignore-file=is:.tags
--ignore-file=is:tags

# Don't search tmp folders
--ignore-dir=tmp

# Don't search git time management
--ignore-dir=gtm

# Don't search byebug history
--ignore-file=is:.byebug_history

# Don't search vcr files
--ignore-dir=spec/vcr
